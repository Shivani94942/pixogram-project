server:
  port: 8890
spring:
  h2:
    console:
      path: /h2
      enabled: true
      settings:
        web-allow-others: true

  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
    
  application:
    name: media-service
    
   
